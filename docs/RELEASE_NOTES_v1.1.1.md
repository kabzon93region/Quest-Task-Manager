# Quest Task Manager v1.1.1

### Исправлено
- **Установка через SideQuest:** release APK теперь подписан (v1.1.0 был без сертификата → `INSTALL_PARSE_FAILED_NO_CERTIFICATES`).

### Установка

```powershell
adb install -r dist\QTaskMgr-v1.1.1-release.apk
```

Или перетащите APK в SideQuest после пересборки на ПК.
