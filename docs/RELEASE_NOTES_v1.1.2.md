# Quest Task Manager v1.1.2

## Что нового в v1.1.2

- **Исправлен запуск:** устранена рекурсия в логере — приложение зависало на тёмном экране (сотни shell-вызовов при старте).
- **Лог:** очищается при каждом запуске из лаунчера; shell-команды только в logcat.
- Рекомендуется обновиться с **v1.1.0** (без подписи) и **v1.1.1** (зависание при запуске).
