# Quest Task Manager v1.2.0

## Что нового в v1.2.0

- **Закрытие приложений:** исправлен краш Shizuku (`process hasn't exited` при `am force-stop`).
- **Настройки Android:** кнопка открывает главную страницу (`SettingsHomepageActivity`), а не раздел «Система».
