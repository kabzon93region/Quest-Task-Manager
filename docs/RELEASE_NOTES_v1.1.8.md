# Quest Task Manager v1.1.8

## Что нового в v1.1.8

- **Настройки Android:** исправлен формат shell-команды (`com.android.settings/.Settings$…` — был пропущен `/`).
- **Тап по плашке** на вкладке «Приложения»: отдельная зона `appInfoArea` (иконка + текст), переключатели справа не перехватывают клик.
- **Запущенные:** список строится по установленным пакетам с RAM/PS, без лишнего отсечения по `isLikelyPackageName`.
